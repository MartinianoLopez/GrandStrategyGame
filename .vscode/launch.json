{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SDL App",
      "type": "cppdbg",
      "request": "launch",

      "program": "${workspaceFolder}/app",
      "args": [],
      "cwd": "${workspaceFolder}",

      "stopAtEntry": false,

      "environment": [],
      "externalConsole": false,

      "MIMode": "gdb",

      "preLaunchTask": "build"
    }
  ]
}
